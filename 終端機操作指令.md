# 終端機操作指令

與根目錄 **`README.md`**、`技術棧與架構總覽.md` 同層，方便快速查常用命令。

---

## 環境準備

```bash
# 安裝依賴（包含 Tailwind CSS、DaisyUI、PostCSS）
npm install

# 安裝完成後，以下功能即可使用：
# - i18n：支援語言：繁體中文 (zh_TW)、英文 (en_US)
# - Tailwind CSS：Utility-first CSS 框架
# - DaisyUI：組件庫（基於 Tailwind）
# - Vitest：單元測試框架
# - 自動導入：Vue API 和組件自動導入
# - Git Hooks：預提交程式碼檢查

# 升級所有 npm 套件到最新版本（會改 package.json 到最新版，含 major；單純 npm update 只會在現有 semver 範圍內更新）
npx npm-check-updates -u
npm install

# 初始化 Husky（首次安裝後執行）
npm run prepare
```

## 開發模式

```bash
# 啟動開發伺服器（預設 port 6600）
npm run dev

# 指定 port 啟動
VITE_FRONTEND_PORT=3001 npm run dev

# 啟用 HTTPS（需要 certs 目錄中的憑證）
VITE_HTTPS_ENABLED=true npm run dev
```

## 建置與預覽

```bash
# 型別檢查 + 建置（生產環境）
npm run build

# 僅建置（不進行型別檢查）
npm run build-only

# 預覽建置結果
npm run preview
```

## 測試

```bash
# 執行單元測試（Vitest）
npm run test

# 單次跑完即結束（腳本／CI 用，不進 watch）
npm run test:run

# 執行測試並開啟 UI 介面
npm run test:ui

# 執行測試並生成覆蓋率報告（含門檻；與 CI 相同）
npm run test:coverage
```

### 覆蓋率（首次設定與查看）

```bash
# 產生覆蓋率報告（未達 `vitest.config.ts` 內 thresholds 會失敗）
npm run test:coverage
```

- 門檻（全專案彙總）：**lines／statements ≥ 45%**、**branches／functions ≥ 35%**（可日後調高）。
- 報表摘要會顯示在終端機（Statements / Branches / Functions / Lines）。
- HTML 報表位置：`coverage/index.html`（由專案根目錄開啟）。

## 程式碼品質

```bash
# 格式化程式碼（Prettier）
npm run format

# 僅檢查格式是否符合 Prettier（不改檔；CI 使用此指令）
npm run format:check

# 程式碼檢查（ESLint，含 Vue / TypeScript）
npm run lint

# 型別檢查（TypeScript）：同時檢查應用程式與測試檔
# - type-check:app：vue-tsc --build（對應 tsconfig.app，不含 __tests__）
# - type-check:tests：vue-tsc --noEmit -p tsconfig.vitest.json（spec 內 @/ 與 vitest/globals）
npm run type-check

# 僅檢查應用程式或僅檢查測試（除錯用）
npm run type-check:app
npm run type-check:tests

# 提交前一條指令（format + lint + type-check）
npm run precommit
```

## 提交前檢查指令

提交前執行一條即可（格式化 → lint → 型別檢查，依序執行）：

```bash
npm run precommit
```

建議提交前另跑：`npm run lint`、`npm run type-check`、`npm run test:run`（`precommit` 不含測試）。

若後端 API 有異動，請先啟動後端並產生型別，再跑 precommit：

```bash
npm run generate:api
npm run precommit
```

提交時 Husky 會自動執行 `lint-staged`，對已暫存檔案做 Prettier 格式化。

## Git Hooks

```bash
# Git Hooks（透過 Husky 配置）
# 預提交時會自動執行 lint-staged：
# - *.{js,ts,vue}：Prettier 格式化 + ESLint --fix
# - *.{json,md}：Prettier 格式化

# 如需手動執行預提交檢查
npx lint-staged
```

## 樣式設定

```bash
# Tailwind 與 DaisyUI 主題設定位於 src/assets/main.css

# 切換主題（在版面層的 data-theme 屬性）
# 目前專案內建主題：brainwave、brainwave-dark（僅兩個自訂主題）
```

## API 型別產生（OpenAPI → TypeScript）

從本地後端 OpenAPI 規格產生 `src/api/generated/schema.d.ts`。流程：先啟動後端 -> 執行 `npm run generate:api` -> 將產出檔一併提交版本控制，Vercel 直接使用儲存庫內檔案即可。

```bash
# 從 http://localhost:8080/v3/api-docs 取得規格（需先啟動後端）
npm run generate:api
```

## 環境變數

### 開發環境（.env 或環境變數）

```bash
# 前端服務 port
VITE_FRONTEND_PORT=6600

# 前端呼叫 API 的基底網址
VITE_API_BASE_URL=http://localhost:8080

# 本地開發伺服器 proxy 目標（供 Vite dev server 使用）
VITE_BACKEND_API_URL=http://localhost:8080

# 前端 Context Path
VITE_FRONTEND_CONTEXT_PATH=/

# 是否啟用 HTTPS
VITE_HTTPS_ENABLED=false

# API 超時時間（毫秒）
VITE_BACKEND_API_TIMEOUT=60000

# 預設分頁大小
VITE_PAGE_SIZE=10

# 系統識別碼
VITE_SYSTEM_ID=InsightEdge-ES-TestData
```

## 常用指令組合

```bash
# 開發模式（PowerShell）
$env:VITE_FRONTEND_PORT=6600; npm run dev

# 後端 API 有異動時：先啟動後端，再產生型別
npm run generate:api

# 建置並預覽
npm run build && npm run preview

# 格式化並型別檢查
npm run format && npm run type-check

# 測試並生成覆蓋率
npm run test:coverage

# 完整檢查（格式化 + 型別檢查 + 測試）
npm run format && npm run type-check && npm run test
```

## 自動導入功能

專案支援自動導入，以下 API 無需手動 import：

```typescript
// Vue API（自動導入）
;(ref, reactive, computed, watch, watchEffect)
;(onMounted, onUnmounted, onUpdated)
;(provide, inject)
;(useRouter, useRoute)
useI18n
;(defineProps, defineEmits, defineExpose)

// 組件（自動導入）
// src/shared/components 目錄下的組件可直接使用，無需 import
// 例如：<Button>、<Card>、<Input>、<DataTable>、<Breadcrumb> 等
```

## 路由結構

- **登入導向**：後台登入成功後預設進入儀表板（`/dashboard`）。

```bash
# 主要路由
/                    # Landing 首頁（公開頁）
/dashboard           # 後台首頁（儀表板）
/users               # 使用者管理（後台管理員）
/members             # 會員管理（前台會員）
/components/guide    # 組件說明
/components          # 組件展示
/tools               # 工具說明（母頁面）
/tools/files         # 檔案處理工具
/tools/format        # 格式化工具
/tools/validation    # 驗證工具
/examples            # 實例說明
/examples/api        # API 客戶端實例
/examples/form       # 表單控制實例
/examples/store      # 狀態管理實例
/about               # 關於頁面

# 錯誤頁面
/404                 # 404 錯誤
/401                 # 401 未授權
/500                 # 500 伺服器錯誤
```

## 相關文件

- 架構與流程：**docs/架構與請求流程.md**
- 部署：**docs/前端部署指南.md**

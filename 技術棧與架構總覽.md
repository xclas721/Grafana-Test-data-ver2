# 技術棧與架構總覽（前端）

本文件說明 **本 repo（Brainwave Frontend）** 採用的技術、目錄概念與自動化流程，供對外說明或交接，與根目錄 **`README.md`** 同層以便一眼找到。細節仍以 **`docs/`** 內其他文件（如架構與請求流程、認證、部署）為準。

---

## 1) 執行環境與框架

- **Node.js**：`^20.19.0 || >=22.12.0`（見專案根目錄 `package.json` 的 `engines`）
- **Vue 3**（Composition API）
- **TypeScript**
- **Vite**（建置與開發伺服器）
- **Vue Router 5**
- **Pinia**（狀態：主題、語系、認證、載入等）

---

## 2) UI 與體驗

- **Tailwind CSS v4**（含 **@tailwindcss/vite**）
- **DaisyUI v5**（多主題／元件風格）
- **Vue I18n**（繁中、英文等）
- **Luxon**（日期時間）
- **@vueuse/core**
- **SweetAlert2**（提示）
- **lucide-vue-next**（圖示）

---

## 3) 與後端 API 契約

- 透過 **環境變數** 設定 API 基底位址等（例如 `VITE_API_BASE_URL`，預設 `http://localhost:8080`）
- 腳本 **`generate:api`**：使用 **openapi-typescript** 由 OpenAPI 產生型別（流程見 `scripts/generate-api.mjs` 與 **`docs/介面契約同步說明.md`**）

---

## 4) 專案結構（概念）

- `src/pages`、`src/layouts`、`src/router`：頁面與路由
- `src/shared/services`、`src/shared/utils`、`src/shared/components`：共用服務、工具、元件
- `src/stores`：Pinia stores
- `src/locales`：i18n 資源

更完整的樹狀說明見專案根目錄 **`專案樹狀架構.md`**。

---

## 5) 建置、品質與 CI

- **vue-tsc**：應用與測試專案型別檢查（`type-check`、`type-check:app`、`type-check:tests`）
- **ESLint** + **eslint-plugin-vue**、**typescript-eslint**
- **Prettier**
- **Vitest** + **@vue/test-utils** + **jsdom**（單元／元件測試）
- **Husky** + **lint-staged**（本地 pre-commit 流程）
- **GitHub Actions**（`.github/workflows/ci-frontend.yml`）：於 **Node 20、22**（matrix）執行 `npm ci` → `type-check` → `lint` → **`test:coverage`（Vitest 門檻）** → `format:check` → `build`
- **Vercel**：專案根目錄有 `vercel.json`，並有 CD workflow；步驟見 **`docs/前端部署指南.md`**

---

## 6) 與後端服務搭配（選讀）

本應用預設透過 `VITE_API_BASE_URL` 連線至 **Brainwave 後端**（Spring Boot 多模組專案）。後端技術總覽見該專案**根目錄** `技術棧與架構總覽.md`；API、認證與部署等見該專案 **`docs/`**（例如 **`docs/後端部署指南.md`**）。

---

## 7) 驗證方式（精簡）

於本 repo 根目錄執行 `npm run type-check`、`npm run build`、`npm run test:run`；其他指令見根目錄 **`終端機操作指令.md`**。

---

_版本號以 `package.json` 為準；技術選型變更時請同步更新本檔。_
